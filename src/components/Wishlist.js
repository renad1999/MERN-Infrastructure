import React from "react";


const Wishlist = ({  }) => {
  return (
    <div>
      <h1>Wishlist</h1>
      <ul>
        {/* {wishlist.map((item, idx) => (
          <li key={idx}>
            <p>{item.name}</p>
            <p>£{item.price}</p>
          
          </li>
        ))} */}
      </ul>
    </div>
  );
};

export default Wishlist;
